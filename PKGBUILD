# Maintainer: Thiago Perrotta <perrotta dot thiago at poli dot ufrj dot br>
pkgname=dotfiles-thiagowfx
pkgver=1
pkgrel=1
pkgdesc="thiagowfx's dotfiles"
arch=('any')
url="https://github.com/thiagowfx/dotfiles/"
license=('GPL')
depends=(
        'git' 'mercurial' 
        'i3-wm' 'i3lock-wrapper' 'quickswitch-i3'
        'python' 'r'
        'bash' 'zsh'
        'firefox' 'termite'
        'gnupg' 'openssh' 'keychain'
        'rcm'
        'xorg-xinit' 'xorg-server' 'xorg-setxkbmap' 'xorg-xsetroot' 'xorg-xrdb' 'xorg-xmodmap'
        'unclutter'
        'parcellite' 'feh' 'mpd'
        'pulseaudio' 'pulseaudio-alsa' 'ponymix' 'pulseaudio-ctl' 'pavucontrol'
        'compton' 'redshift'
        'network-manager-applet'
        'sxhkd'
        'mutt' 'msmtp' 'msmtp-mta'
        )
makedepends=()
optdepends=()

package() {
    :
}
