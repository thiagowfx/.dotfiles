# Maintainer: Thiago Perrotta <tbperrotta@gmail.com>
pkgname=thiagowfx-dotfiles-meta
pkgver=1
pkgrel=1
pkgdesc="Metapackage for an enhanced experience of thiagowfx's dotfiles"
arch=('x86_64')
url="https://github.com/thiagowfx/.dotfiles"
license=('BSD')
groups=('thiagowfx')
depends=(
	# CLI/passive
	'highlight'
	'lesspipe'
	'shellcheck'

	# CLI/active
	'ack'
	'dtrx'
	'exa'
	'fpp'
	'fzf'
	'moreutils'
	'tmux'
	'trash-cli'
	'tree'

	# Meta
	'git'
	'make'
	'stow'

	# Shell
	'bash'
	'bash-completion'
	'zsh'
	'zsh-completions'

	# Terminal Emulator
	'alacritty'

	# Text Editor
	'vi-vim-symlink'
	'vim'

	# VCS
	'git'
	'tig'

	# X11
	'dex'
	'dunst'
	'rofi'
	'xclip'
)
makedepends=(
	'git'
	'make'
	'stow'
)

package() {
	:
}
