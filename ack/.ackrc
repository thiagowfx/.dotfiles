# expose column for better ctrl/cmd + click navigation for text editors
--column

# chromium grd files
--type-add=grd=.grd,.grdp

# generate ninja: https://gn.googlesource.com/gn/
--type-add=gn=.gn,.gni

# ini
--type-add=ini=.ini

# markdown
--type-add=md=.md,.markdown

# shell
--type-add=sh=.sh,.bash.,zsh

# text
--type-add=txt=.txt

# chromium
--ignore-dir=out

# nodejs
--ignore-dir=.wireit
