#!/bin/bash
#
# Custom aliases

# colors
alias diff="diff --color=auto"
alias grep="grep --color=auto"
alias ip="ip --color=auto"

# file listing
alias ls="ls -Fh --color=auto"
alias la="ls -Al"

# typos
alias gi="git"
alias gt="git"
alias sl="ls"
