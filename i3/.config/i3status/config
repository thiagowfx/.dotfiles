general {
	interval = 5
}

order += "load"
order += "wireless _first_"
order += "tztime local"

load {
	format = "%5min"
}

wireless _first_ {
	format_up = "%essid %ip"
	format_down = "wifi down"
}

tztime local {
	format = "%Y-%m-%d %H:%M"
}
