general {
	interval = 5
}

order += "memory"
order += "load"
order += "wireless _first_"
order += "tztime local"

memory {
	format = "%used"
	threshold_degraded = "15%"
	format_degraded = "memory: < %available"
}

load {
	format = "%1min"
}

wireless _first_ {
	format_up = "%essid %ip"
	format_down = "wifi down"
}

tztime local {
	format = "%Y-%m-%d %H:%M"
}
