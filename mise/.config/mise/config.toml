# Global mise configuration: tool versions, env vars and tasks.
# https://mise.jdx.dev/configuration.html
#
# Per-project versions come from each repo's mise.toml, or from idiomatic
# version files (see settings below) for repos that do not use mise yet.

[tools]
# keep-sorted start
go = "1.27"
node = "26.7"
# Ships bin/python next to bin/python3, so no `python` shim formula is needed.
# Project virtualenvs still come from uv.
python = "3.14"
ruby = "3.4"
terraform = "1.16.1"
terraform-docs = "0.24.0"
# keep-sorted end

[env]
# Go CLI binaries installed with `go install`.
_.path = "{{env.HOME}}/go/bin"
# keep-sorted start
# mise points GOBIN at its own versioned install directory, which a go
# upgrade deletes. Keep `go install` output in the directory on _.path above.
GOBIN = "{{env.HOME}}/go/bin"
GOPATH = "{{env.HOME}}/go"
# https://stackoverflow.com/questions/46288847/how-to-suppress-pip-upgrade-warning
PIP_DISABLE_PIP_VERSION_CHECK = "1"
# keep-sorted end

[settings]
# Honour .go-version, .node-version, .python-version, .ruby-version,
# rust-toolchain.toml and .terraform-version in repos without mise.toml.
idiomatic_version_file_enable_tools = [
  # keep-sorted start
  "go",
  "node",
  "python",
  "ruby",
  "terraform",
  # keep-sorted end
]

# vim: ft=toml
