#!/bin/sh

dmode="$(cat /sys/class/drm/card0-VGA-1/status)"

if [ "${dmode}" = connected ]; then
	xrandr --output VGA1 --auto --above eDP1
else
	xrandr --auto
fi

[ -e ~/.fehbg ] && command -v feh &>/dev/null && eval $(cat ~/.fehbg)
