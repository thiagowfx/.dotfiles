---
- name: Bootstrap environment
  hosts: all
  roles:
    - role: elliotweiser.osx-command-line-tools
    # keep-sorted start
    - role: brew
    - role: brew_cask
    - role: macos
    # keep-sorted end
