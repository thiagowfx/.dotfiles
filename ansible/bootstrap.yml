---
- name: Bootstrap environment
  hosts: all
  roles:
    - role: elliotweiser.osx-command-line-tools
    # keep-sorted start
    - role: brew
    - role: brew_cask
    - role: iterm2_shell_integration
    - role: macos
    # keep-sorted end
